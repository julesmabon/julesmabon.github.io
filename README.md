My personal nightmare of a website

documentation:
- https://jekyllrb.com/docs/
- https://jekyllrb.com/docs/installation/


install packages with 
```bash
bundle install
```

serve with 
```bash
bundle exec jekyll serve
```


install gems with `gem install <gem> --user-install`